todo:

- [ ] Potentially use blue tokenizer/lexer, cant really use scanner
    - strip it down to just idents, numbers, and strings
    - when tabs are found, increment twOffset
    - only column pos (the position in the line) is needed, and can add twOffset to it so dont need to recalc or do something else weird
    - and idents could just use hldb (string would need to be handled on its own in there)
    - [ ] Handle multiline string correctly (need to move it out of strHl or something)
- [ ] Missing highlight color for numbers, and comments
- [ ] Add -d or -debug flags, when enabled, capture timing of all functions and record in the log
- [ ] Allow movement keys to be configurable (if including "modes")
- [ ] Macros, record into small buffer via event keys
    - [ ] replay macro by sending those keystrokes onto screen.EventQ()
- [ ] should fallback to os.Stderr when log file failed to be created?
- [x] main uses tcell.EventKey so things like KeyQ are not captured I think
- [x] Fix horizontal movement (mostly viewport)
- [x] Fix vertical movement (should be able to move directly up a line and not clamp back to 0)
- [x] Handle {},!+=/*%   etc. (need to hl these with kw1 color)